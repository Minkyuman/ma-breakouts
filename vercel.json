{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ \"$RUN_DB_MIGRATIONS\" = \"true\" ]; then npm run db:migrate && npm run db:verify-research; fi; if [ \"$RUN_CLASSIFICATION_CACHE_WARM\" = \"true\" ]; then tsx scripts/warm-market-ranking-classifications.ts; fi; NITRO_PRESET=vercel npm run build",
  "outputDirectory": ".output",
  "regions": ["icn1"],
  "crons": [
    {
      "path": "/api/internal/search-index/refresh",
      "schedule": "0 20 * * *"
    }
  ]
}
